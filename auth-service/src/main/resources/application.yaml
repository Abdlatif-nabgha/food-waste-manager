server:
  port: 8081
google:
  client-id: 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
spring:
  application:
    name: auth-service
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/food_waste_manager_db}
    username: ${SPRING_DATASOURCE_USERNAME:nabgha}
    password: ${SPRING_DATASOURCE_PASSWORD:44026676}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  mail:
    host: ${MAIL_HOST:smtp.gmail.com}
    port: ${MAIL_PORT:587}
    username: ${MAIL_USERNAME}
    password: ${MAIL_PASSWORD}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
jwt:
  secret: ${JWT_SECRET}
  expiration: 86400000  # 24h en ms